#goal : Run and see hello workflow.

Name: Hello workflow

On: Push

Jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "Hello from Github actions"
        