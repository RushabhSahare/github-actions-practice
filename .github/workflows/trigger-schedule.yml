name: Scheduled Job

on: #0 9 * * 1 for everymonday at 9 AM UTC
  workflow_dispatch:

jobs:
  daily-check:
    runs-on: ubuntu-latest
    steps:
      - name: Print current time
        run: echo "Scheduled run at: $(date -u)"